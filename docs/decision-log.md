# Decision Log

Short, owner-facing record of repo-level decisions that affect packet sequencing or shared tooling.

Entry convention: new decisions should use a `**Date:** YYYY-MM-DD` field or an equivalent dated bullet so future agents can distinguish accepted decisions from stale notes.

**Date-stamping rule (added 2026-08-10):** always take the date from the environment (`date` / commit timestamps), never from conversation recency — async sessions span days. Correction applied 2026-08-10: the star-series entries (plans 111/112/113 gates and acceptances, plan-97 resolution) were mis-dated 2026-07-22 from conversation context; their true dates (2026-08-05/06/10 per commit evidence) are now restored. The per-team keying entry (2026-07-21) is best-effort within a one-day uncertainty.

## Accepted decisions

- 2026-07-07: Adopt Bootstrap packet-status tooling in phases, starting with the core status scripts and local consumer manifest.
- 2026-07-07: Keep existing packet docs in place for now; frontmatter migration and generated-index conversion are deferred to Plan 88.
- 2026-07-16: Reframe the internal `strategy-brain` project as the student-facing **Field Decisions** arc. Keep one Blockly-controlled ally and the persistent shared workspace; preserve the `strategy-brain` id; reserve runner-index role coordination for Team Strategy Script; handle Challenge 28 live-human evidence separately.
- 2026-07-21: Usage Tracker V2 run-version store values (executing Plan 84 B6 for plans 106–110): total byte budget **~2 MB** (owner-selected); per-level guided version cap **K = 5** (keep first + last + most-recent-5 unique runs; owner-selected from options K = 3 / 5 / 8).
- 2026-07-21: Usage Tracker V2 age-eviction posture: the durable learning ledger and run-version store are **exempt from age-based eviction** (bounded by count/budget windows only); the v1 7-day / 20-session age rules apply only to ephemeral churn and raw event tails. Recorded as the only posture consistent with Plan 84's settled D1/D2 cross-session promises; a 90-day backstop was offered and not selected. Owner may override by amending this entry and plans 106/107 before dispatch.
- 2026-07-21: Free-play run-version keying (surfaced by Plan 107 review as the packet's anticipated stop condition): contexts are keyed **per team slot** (`freeplay:team1` / `freeplay:team2`, mirroring the existing per-team stored workspaces), each with its own dedupe chain and its own **~20-version window**. D2's "last ~20" is now per-team depth; total is bounded by the 2 MB byte budget. Guided levels are unaffected (single visible workspace).
- 2026-07-22: Similarity-detection semantics (surfaced by Plan 109 review): **ratify import-forensic semantics** — the "similar event sequence" flag requires identical attempt sequences AND identical captured program states, so it is strong evidence when it fires but rare by design; "not flagged" must not be read as independent work. Label wording in CLI and admin UI sharpened to say so. Threshold-based or code-aware similarity is deferred to a future dedicated design packet (recorded in the future-directions backlog). Rejected now: stripping `xmlHash` from the fingerprint (noisy on-rails false positives) and normalizing block ids out of the usage hash (real design work, needs false-positive analysis).
- 2026-07-22: Rewrite-aware ledger field names (Plan 110 acceptance): Plan 91 named the star/par/mastery group but not its field names, so the implementer's concretization is ratified here: `starsEarned` (0–3 aggregate), `parBeaten` + `turnPar`, `masteryAchieved` + `masteryCriterionId`, and `filmReviewSummary` for the S7 recap. Plan-96 writes against these names. **Rename window:** if the owner wants different names, change them before plan-96 dispatch — after producers write them, renaming becomes a migration. Closed vocabularies are documentary at the receptacle layer; plan-96 owns validation.
- 2026-08-05: Star-2 par semantics (plan-111 gate): star 2 is earned at `turnsSpent <= turnPar` — par is the already-generous number, so reaching it earns the star. The charter's "beat a generous turn par" is satisfied by baking the buffer into the par value. Permanent rule for plan-113's campaign-wide authoring.
- 2026-08-05: Mastery-criterion discriminating-power standard (plan-111 gate): a level's `masteryCriterionId` should discriminate — if the level's mechanic is mandatory to pass at all (per an S8 fixture or simulation), `concept-used` awards star 3 to every pass and adds no information. Plan-111's pilot and plan-113's authoring must check discriminating power per level (simulation: can the level be passed without the concept action?) and choose a discriminating criterion where one exists, or document "no honest criterion — 2-star max."
- 2026-08-05: Pilot levels are 2-star max (plan-111 gate, owner decision): `bring-it-home`, `enemy-nearby`, and `jump-the-gap` are all concept-mandatory by simulation evidence, and the only closed-vocabulary discriminator available (`under-block-budget`) is code golf at discriminating tightness — which S6 explicitly forbids as a default ("the AP CSA bridge is clear correct conditions, not code golf"). The pilot levels therefore carry `turnPar` only, with "no honest criterion — 2-star max" documented. This is the plan-113 standard: mandatory-mechanic levels stay 2-star max unless a discriminating, non-golf criterion exists; expanding the closed vocabulary is a charter conversation, not a packet decision.
- 2026-08-05: Star tiers are cumulative (plan-111 acceptance): `starsEarned` is 3 only when both `parBeaten` and `masteryAchieved` hold, 2 only when `parBeaten` holds; a mastery-meeting but slower-than-par run earns 1 star with `masteryAchieved: true` still recorded in the ledger. Ratified as the natural reading of S6's ⭐/⭐⭐/⭐⭐⭐ model; plan-113 authors criteria against this semantics.
- 2026-08-05: Star display copy and placement (plan-112 gate, owner approval): result-banner copy approved as — 1-star: "Level passed! ★☆ — Finished in {turnsSpent} turns. Beat par ({turnPar} turns) to earn a second star!"; 2-star: "Level passed! ★★ — Finished in {turnsSpent} turns (par is {turnPar}). Par beaten!"; 3-star: "Level passed! ★★★ — Finished in {turnsSpent} turns. Par beaten and mastery challenge completed!" (corrected from the implementer's imperative slip); pass-only/no-metadata: "Level passed! ★"; failure shows no star content. Level picker shows empty slots (☆☆) on unpassed levels ONLY where star metadata offers them; pass-only and no-metadata levels show nothing until passed.
- 2026-08-05: Inversion level prototype direction (plan-97 gate resolved): placement = Optional Lab at campaign end; interaction shape = reuse prediction-checkpoint multiple-choice mechanics (locked program + 2–3 board/outcome choices, student commits, then sees the run). Prototype subject: a conditional program whose candidate boards differ on whether the condition ever fires. One level, optional, no campaign renumbering.
- 2026-08-06: Inversion prototype must be a BOARD choice (plan-97 preflight review, owner ruling): the implementer's first design (predict the ally's action on one fixed board) was rejected as structurally a fourth prediction checkpoint. The prompt must present 2–3 board options and ask which board the locked program passes on — reasoning from code to board, not code to behavior on a given board. Same radio mechanics; the locked program stays non-editable (enforcement mechanism to be named in implementation); the level file carries the plan-113 non-runnable pass-star-only comment.
- 2026-08-05: Plan-113 gate decisions (owner): (a) **Turn-limit bumps approved** — `bughunt-28` maxTurns 12→15 (turnPar 14), `index-jobs` 10→12 (turnPar 11); packet scope amended to include these two mechanics changes. (b) **advanced-scrimmage star metadata deferred** — pass-star-only this round with a comment citing the documented step-9 checkpoint fixture debt (`guided-project-solutions.test.js:31-45`); do NOT tune the level to stale evidence. (c) **Star-3 shape ratified** — this round star 3 exists only on Phase 6 multi-ally levels (`both-allies-active`); collision/waste event tracking deferred to the future-directions backlog as the enabler for richer criteria later.
- 2026-08-10: Advanced-scrimmage repair approach (plan-114 gate, owner decision): **Option 1 — fixture tuning only.** Replace the step-09 and final fixtures with the verified 41-turn passing program (orchestrator-reproduced: PASSED, `win_condition_met`, human runner parked); `maxTurns: 55` unchanged (14-turn student slack). Root cause confirmed: commit `2d9fb27` (owner's flag-placement rule-compliance move, May 20) orphaned the pre-living-board straight-line fixtures. After repair: `turnPar: 48` (41 + 7 per the ratified formula) and `masteryCriterionId: "both-allies-active"` with discriminating evidence (idled support allies → carrier intercepted), closing the plan-113 deferral.
- 2026-08-10: Plan-115 triage approved in full (owner): adopt packet-status-system resync to 1.3.0, agent-starting-prompts to 1.5.0, review-response-tiers 1.0.0, commit-discipline 2.0.0, subagent-delegation 2.0.0 (prose + roster files), and **advisor-consultation 1.0.2** (owner explicitly approved despite upstream's non-declinable core marking; kimi-code is advisorCapable: false upstream, so consultation runs owner-mediated or honestly declared). The date-stamping reverse-flow proposal was written upstream at `C:\AI\Bootstrap\docs\bootstrap-dev\incoming\2026-08-10-browser-battlegorithms-date-stamping-from-environment.md`.
- 2026-08-05: Pass banner keeps the result reason (plan-112 acceptance, owner decision): the humanized result reason follows the star line on pass banners, and `team_scores_point` levels keep their tailored lead ("Scoring a point completed the challenge."). The star sentence always comes first.
- 2026-09-01: **Stage 0 split ratified (owner).** The gate-independent portion of the GAS review synthesis's Stage 1 becomes its own stage and is authorized to proceed ahead of the probe gates: `plan-118` (exception-safe storage adapter + guided in-memory fallback, review F7), `plan-119` (starter-mismatch displaced-work recovery, review F1), `plan-120` (nested-frame and tenant-identity probe kit + the `integrations/` repository surface, Gates 1 and 2), `plan-121` (identity-stripped cloud evidence builder, filename-attribution policy, analyzer blank-name repair, review F6 + owner decision 8). The Codex pre-implementation pass clarified that `plan-121` does not put the exact teacher-download filename grammar in student client code; that server-side grammar waits for the canonical Stage 1 protocol/teacher-extraction surface. Each packet still carries its own in-packet owner gate. Stage 1 proper — protocol/evidence schema, GAS server core, class index, teacher extraction, retention/purge, load probe, pilot — stays unwritten until Gates 0/1/2 return, because the parent-origin authentication design cannot be chosen before origin stability is measured. Sequencing: `plan-118` before `plan-119` (shared accessor); `plan-120` and `plan-121` are write-scope-disjoint and may run concurrently (mode B).
- 2026-09-01: **New code surfaces ratified (owner, with the `integrations/` path still to confirm).** `src/platform/` — dependency-free browser-capability adapters that every other layer may import and that import nothing from `src/core`, `src/ui`, `src/render`, `src/ai`; first member `safeStorage.js` (`plan-118`). GAS source lives outside the Vite build graph, never imported by `src/` and never importing `src/`, with the client half at `src/integration/` communicating only over `postMessage`. Standing rule: when the protocol/evidence schema lands it has exactly one source file, and the Apps Script copy is **generated** from it with a staleness test — hand-maintained parallel copies are prohibited. The specific directory `integrations/google-apps-script/` (synthesis remaining-decision 7) is recommended but not yet confirmed; `plan-120` gate item 1 still asks it.
- 2026-09-01: **Nested-frame probe child ships in `public/` (owner, `plan-120` gate item 2 = option A).** A standalone `public/integration-probe/nested-frame-child.html` diagnostic page — unlisted, `noindex`, collecting and transmitting nothing — is copied to `dist/` by the existing build so the probe measures the real production origin rather than a stand-in. Accepted cost: one unlisted public page on the live site. Probe results split deidentified-tracked (`reports/orchestration/gas-integration-commentary/probe-results/`) from raw (`local/`, git-ignored).

- 2026-09-01: **Gate 0 answered (owner, from existing district knowledge).** (a) Domain-scoped Apps Script web app publishing by a teacher is **permitted** — owner has done it before in this district. (b) Student learning records in a teacher's district Drive are **permitted** under district agreements, provided the data stays within district Drive control. (c) Retention is **unbounded-permitted**: the district has no objection to indefinite retention of student data held within district Drive control. (d) The third-party-storage question was **reclassified from a policy question into a measurement** taken by the `plan-120` Gate 1 probe, on a representative student-OU device, held in reserve as an allowlisting request to IT only if the probe measures *blocked*. Full record and reasoning: `reports/orchestration/gas-integration-commentary/district-it-questions.md`. Only outstanding Gate 0 item: synthetic domain accounts for Gate 2, which do not block Gate 1.
- 2026-09-01: **Retention consequences of the unbounded-retention answer.** Synthesis finding 9 (purge promises exceeding what Drive can guarantee) largely dissolves; Stage 1 no longer needs the Advanced Drive Service as a *compliance* dependency, and the proposal's placeholder 30-day / 90-day windows are superseded rather than merely unconfirmed. Two things survive the change: teacher-triggered purge stays worth building as a convenience and for mistakes (a permission is not an obligation), and **per-cohort record keys are retained** — their retention rationale is gone but the surviving reason is independent, namely that a returning student in a new class should start a fresh record rather than inherit the previous year's. Caveat for a future thread: if the retention answer was informal rather than documented, get it in writing before a pilot spans a school year.
- 2026-09-01: **Partitioned is not blocked (probe-reporting rule).** The Gate 1 storage measurement must report *partitioned*, *unpartitioned*, *blocked*, and *unknown* as distinct outcomes, never collapse them into one pass/fail, and never infer partitioning from an empty bucket. The probe first writes a sentinel to localStorage and IndexedDB at the child URL in a top-level context, then compares reads and independent round-trips in the framed context. *Partitioned* means the verified direct sentinel is absent in the frame while a separate framed sentinel succeeds — expected, normal, **not** a failure, and the origin of the Stage 2 migration problem. *Blocked* means property/open or round-trip access is denied — the review F7 / `plan-118` condition. Incomplete comparisons are *unknown*. Every storage result records device class and organizational unit; a storage result with no device provenance is not evidence, because Chrome policy is applied per OU and teacher and student OUs commonly differ.

- 2026-09-01: **GAS source location confirmed (owner, `plan-120` gate item 1).** GAS source lives in this repository at `integrations/google-apps-script/`, outside the Vite build graph, never imported by `src/` and never importing `src/`, with the client half at `src/integration/` communicating only over `postMessage`. This closes synthesis remaining-decision 7 and the last pre-mutation gate item on `plan-120`; the packet is now fully cleared for dispatch.
- 2026-09-01: **Gate 2 is not blocked on synthetic accounts.** The identity run matrix is re-tiered by what each condition can falsify. Tier A (is server-derived identity nonblank and correct for a non-teacher domain account under execute-as-deployer) is the only architecture-deciding condition and needs exactly **one** non-teacher account — a synthetic account or one real student. Tier B (multi-login in one profile and account switching) is satisfied by the **teacher account plus any one other**, so no second synthetic account is required. Unrostered/late-enrollee behavior requires the Stage 1 roster surface and is deferred to that packet/pilot checklist; the identity-only probe cannot test it. Tier C (renamed account, disabled account) genuinely requires IT provisioning and cannot be done with a real student, but it tests graceful mid-year degradation, not whether the architecture works — it is a pre-pilot checklist item and must not hold Stage 1 packet writing. A real student may stand in for tiers A and B under three conditions recorded in `plan-120`: pass/fail only and no email in any tracked file, no credential entry beyond a student's own normal sign-in, and an honest explanation to the student. Prefer a synthetic account where one exists for repeatability, not privacy — the probe should be re-runnable after a redeploy without scheduling a student.

- 2026-09-01: **`plan-118` gate cleared (owner).** (a) Storage-unavailable banner copy: "This browser is blocking saving. You can keep playing, but your program will be lost if you reload or close this tab." Corrected from the packet's original "when you close the tab" — a reload also loses the work and students reload constantly, so the original would have been a promise the app could not keep. (b) Fallback posture confirmed: when storage works, guided levels behave exactly as today and the in-memory guided map stays inert; the map activates only under storage-unavailable and is never a second source of truth read alongside a working storage. (c) Banner placement: the existing non-blocking notice surface, dismissible, once per page load. Voice note recorded in the packet: this is a systems message, not coaching — the Copy Voice Contract's scout/coach speaker governs student guidance, not honest failure reporting.
- 2026-09-01: **`plan-120` accepted (orchestrator review).** Probe kit delivered and independently verified: 559/559 unit tests, clean build with `dist/integration-probe/nested-frame-child.html` present and no `dist/integrations/`, hygiene test passing with self-verifying positive and negative fixtures and bidirectional import checks, no network calls anywhere in the probe surfaces, `noindex` present, storage writes gated behind explicit clicks rather than page load, and sentinel keys namespaced under `bba:gas-probe:` with cleanup controls. The storage classification is the strongest part: partitioned requires a successful direct top-level sentinel, its absence in the frame, and a successful *different* framed round-trip — empty storage alone reports `unknown`, which is precisely the misreading the packet warned about. Consultation posture declared as Branch C (orchestrator-gate-only). One inline orchestrator edit at review: the child's `postMessage(..., '*')` readiness announcement is correct in a probe (discovering the parent origin is the probe's purpose) but must never reach the product, so the rationale and a do-not-promote warning are now recorded at the call site and in the probe README.

- 2026-09-01: **`plan-120` probe workflow revision reviewed; sent back on report completeness only (orchestrator).** The revised implementation (`739bf9e`, `ac5ea8f`) is accepted on the merits and independently verified: `storageClassification` gates on the operator-declared context so an uncertain pairing reports `unknown` rather than `partitioned`; both `PLAN120_RESULT` blocks were read field by field and contain no email, exact origin, URL, sentinel, domain, or account id; the raw-JSON copy control is gone; the shell validates HTTPS, exact pathname, `event.origin`, the literal `'null'` origin, and probe version; origin readings export as comparison status (`baseline`/`same-as-baseline`/`changed`/`unknown`), preserving the stability evidence Gate 1 needs while keeping values local; version pinned `plan-120-v1` across all three surfaces; 560/560 tests and a clean build with no `dist/integrations/`. **Ratified as a general lesson:** exporting a *comparison* rather than a *value* is the right pattern whenever a measurement must leave the device but the raw reading is sensitive — reuse it in Stage 1 rather than reinventing it. Sent back to `in-progress` for `repair-01`: the advisor disposition record lists eleven recommendations and eleven acceptances with no rejections and no independent-verification reasoning, which the advisor-consultation convention treats as incomplete by definition; one row also records an endorsement of existing behavior as an accepted change. The Sol / `GPT-5 (Codex)` model-identity discrepancy was handled correctly — flagged, not claimed, recorded as advisory input rather than a provider-capability claim — and is explicitly **not** a defect.

- 2026-09-01: **`plan-120` complete.** `repair-01` verified and accepted: the advisor disposition record now carries per-finding independent-verification reasoning, reclassifies the existing-boundaries row as an endorsement rather than an accepted change (ten accepted, one endorsement), and explicitly declares and defends "no recommendations rejected." Two of its verification claims were spot-checked against the pre-repair code rather than taken on trust and both held — the old child page did let a single operator confirmation mark both storage APIs passed, and the old shell accepted `http:` as well as HTTPS with no pathname or version check. Repair scope was honored exactly: one section of one file, no code touched, no revalidation. Running the probes is an owner action outside the packet; Gate 1 and Gate 2 stay open in `docs/open-questions.md`.
- 2026-09-01: **Packet report folders must match the packet document slug (authoring rule).** `plan-status.js lint` derives the expected `reports/development/<slug>/` folder from the packet filename, so a packet that declares a shortened folder emits a lint warning the moment it completes — breaking the clean-lint baseline with no other symptom. Found when `plan-120` completed. Both offending packets were mine: `plan-120-gas-probe-kit/` was renamed to `plan-120-gas-probe-kit-and-integration-surface/` with all references updated, and `plan-121`'s declared folder was aligned pre-emptively before implementation. The packet-guidance "unless the packet states otherwise" clause is narrower than it reads: the tool does not honor a shortened override. Do not declare a report folder that differs from the packet document slug.

- 2026-09-01: **`plan-121` gate cleared (owner); blank-name handling resolved as Option 1 — the discriminator changes, not the wording.** A *similarity group* is a set of files whose entire canonical event sequence matches (`getUsageEventFingerprint`); the typed name is deliberately excluded from the fingerprint and never affects grouping. It is used only in the second step, which reports a group when its labels are not all identical. With blank names those labels become `submission-1`, `submission-2` — trivially distinct — so every group fires as "identical attempt sequence under **different names**," asserting a name comparison that never happened to a teacher weighing academic integrity. Resolution: **submitter identity** becomes the discriminator — typed name when present, otherwise the caller-supplied file identity, which under owner decision 8 is where authenticated account attribution will live, so this survives contact with Stage 1 rather than needing a second pass. `submission-N` is positional and may never satisfy the distinctness test. Groups whose submitters cannot be distinguished are still reported (the signal is rare and real) with the wording "identical attempt sequence, submitters not distinguishable from these files." Suppression was considered and rejected as discarding evidence. Direct-mode behavior confirmed unchanged.
- 2026-09-01: **`package.json` is serialized between concurrent packets (orchestration rule).** `plan-118` and `plan-121` are otherwise write-scope-disjoint, but both would add test files to the single `test:unit` line — shared state, which commit discipline says cannot be handled concurrently regardless of care. Rule applied: the packet that started first owns the shared file. `plan-118` keeps `package.json`; `plan-121` must not touch it, names its new test files in the progress report, validates by running them directly, and the orchestrator registers them and runs the full suite at review. Reusable whenever two packets are disjoint except for a registration line.

- 2026-09-01: **`plan-120` reopened for `repair-02` — probe behavior changed post-completion without a version bump.** Re-verification of `2cc0c5f`/`66cc484` at owner request confirmed the handshake repair is correct and the bug was severe: `HTMLIFrameElement.sandbox` is a `DOMTokenList` with no `.tokenList` property, so `Array.from(frame.sandbox.tokenList)` threw on every run — *after* `clearTimeout(handshakeTimer)` — so the shell never posted `BBA_PROBE_CONTEXT`, the child never armed the framed storage path, and no timeout or error surfaced. Silent total handshake failure on every attempt. Also re-verified intact: context-gated storage classification, and both result blocks re-audited field by field (the new `browser_family`/`browser_major`/`os_class` are coarse derived values, never the raw user-agent). 566/566 tests, clean build. **Defect:** all three surfaces still report `plan-120-v1`, which (a) defeats `directions.md` step 3, the operator's only build check, (b) lets a hand-pasted stale shell pass the shell↔child version check and fail silently again, and (c) makes returned result blocks indistinguishable by provenance — broken shell, fixed shell, and cached child all read v1. Repair: bump all three to `plan-120-v2`, assert in the hygiene test that the three literals are *equal to each other* rather than hard-coded, instruct a full re-paste of both Apps Script projects, and discard any v1 result block. **General lesson: a measurement artifact's version is provenance, not decoration.** When probe behavior changes, the version must change, or collected evidence cannot be attributed to a build.
- 2026-09-01: **Completed packets that later change behavior get reopened, not amended in place.** `plan-120` was `complete` when `2cc0c5f` altered probe logic; its resolution string then described a build that no longer existed. Reopening to `in-progress` for `repair-02` is the correct lifecycle move — a `complete` status asserts that the delivered artifacts were verified, and that assertion is false once the artifacts change. Docs-only follow-ups to a complete packet are fine; behavior changes are not.

- 2026-09-01: **`plan-120` complete (second closeout).** `repair-02` verified: all three surfaces read `plan-120-v2`; the hygiene test extracts each literal and asserts the three are *equal to each other* rather than matching a hard-coded string, so it survives future bumps while catching one-surface drift; `directions.md` verifies v2, requires re-pasting and redeploying **both** Apps Script projects before the next run, and states the v1 discard rule; `TEMPLATE.md` requires v2. Repair scope was honored exactly — the three probe files changed by one line each (the version literal) and no probe logic moved. 571/571 tests, clean build, `dist` carries v2, no `dist/integrations/`. The earlier v1 run is recorded as provisional context and will be rerun.
- 2026-09-01: **Disjoint write scopes do not give disjoint validation (concurrency lesson).** The `plan-120` implementer reported "3 unrelated existing `preferences` failures" during its full `npm test`. They were unrelated to `plan-120` but they were **not** pre-existing: `plan-118` was concurrently mid-edit on `src/ui/preferences.js` in the shared worktree, and `npm test` reads the whole tree regardless of who owns which files. Once `plan-118` landed, `preferences.test.js` returned to 9/9 and the full suite to 571/571. The mode-B concurrency rule reasons about *writes*; validation *reads* everything. Standing correction for future concurrent dispatch: a packet's own targeted tests are its authoritative validation, and any full-suite failure must be attributed by file with `git status` before being described — "existing failure" asserts a red baseline and must never be used for another packet's in-flight work.

- 2026-09-01: **`plan-118` complete.** Verified independently rather than from the report: `rg` confirms no module outside `src/platform/safeStorage.js` touches `window.localStorage`; the guided in-memory map is strictly gated behind `!isLocalStorageAvailable()` at all three sites and inert whenever storage works; the Plan 45 starter-version compare is correctly skipped in the memory-only path; banner copy matches the approved sentence verbatim in markup and in the subsystem note; 571/571 unit tests and a clean build, and the new Playwright spec passes when run by the orchestrator (1/1). The implementer found and migrated `resetWorkspaceToCurrentStarter`, a third storage writer the packet requirements had not named. **The preflight-added guided-progress test came back stronger than specified:** it asserts every level beyond the first stays `LOCKED` under throwing storage, completes a level to confirm in-session advance still works, then re-initializes a fresh app and asserts the level is `LOCKED` again — proving the write did not persist rather than merely that it did not throw. One tier-1 orchestrator correction at review: the progress report listed a `clearLocalStorage()` export that does not exist anywhere in the codebase; corrected inline, because a durable record of a new shared module's API is something later packets read and rely on.
- 2026-09-01: **`src/platform/safeStorage.js` holds process-global memoized state, and the unit suite runs `--test-isolation=none` (caution, not a defect).** `cachedAvailable`, `customStorage`, and `hasLoggedFailure` are module-level and shared across every unit test file in one process. `plan-118` handled the three known cases correctly (`preferences.test.js` gained `beforeEach`/`after`, `guided-progress.test.js` uses try/finally, `safe-storage.test.js` resets after every test), and later files were confirmed to pass standalone as well as in-suite, so ordering is not currently load-bearing. But any future test that sets storage state must reset it, or it will silently change behavior for every file after it in `package.json`'s explicit list.

- 2026-09-01: **`plan-119` gate cleared (owner) — all five items.** (1) Recovery UX: non-blocking notice plus a restore button, shown only when a displaced copy exists for the current level; load-time modal and silent-preservation-without-notice both rejected. (2) Copy: notice "This level's starter program was updated, so your earlier program was set aside." and button "Restore earlier program" — **the implementer's "starting blocks" was corrected to "starter program"** because that is the term the app already shows students on the reset control (`index.html:133`), and one concept should not carry two names. (3) Cap 8, oldest-first by `displacedAt`, one slot per level; accepted as recommended without being put to the owner as contested, since packet and implementer already agreed. (4) Reversibility: the recovery copy is **never explicitly deleted** — only superseded or pruned. This overrides the implementer's clear-on-first-edit proposal: it is strictly safer, and the Blockly change event fires on cosmetic drags (review F14), so a student who restored and then nudged a block would silently lose the copy. No change-event hook. (5) Failure copy: plain-language pair stating what happened and what survived, with "the starter update was not applied" replaced. All four strings are systems messages in plain second person, following `plan-118`'s precedent that the Copy Voice Contract's scout/coach speaker governs guidance, not failure reporting.
- 2026-09-01: **`plan-119` browser spec is required, not optional (orchestrator addition).** The preflight plan proposed manual browser verification only. Precedent exists both ways: `plan-118` shipped `tests/browser/storage-resilience.spec.js`, and `tests/browser/workspace-starter-versioning.spec.js` already pre-seeds a stale version key with an unmatched hash via `addInitScript` — the exact fixture this packet needs. The spec must prove the notice appears with approved copy, the restore button is keyboard reachable and restores, and **a reload afterwards still shows the restored program**, since that last assertion is what actually proves the version key was re-stamped and is the most likely thing to be got wrong.

- 2026-09-01: **`plan-119` complete.** Verified independently: the preservation-failure path returns `storedXml` and writes nothing, so the only durable copy is never overwritten when the recovery write fails; fail-safe ordering confirmed (starter write plus read-back precede the version stamp); restore writes XML, verifies, stamps the version key, verifies, and only then touches the live workspace; cap/prune removes unknown levels, enforces one slot per level, and deletes the orphan slot when the index write fails; all four approved strings appear verbatim; the "silent by design" paragraph is gone from the subsystem note. 584/584 unit tests, clean build, and 6/6 browser tests when run by the orchestrator, including keyboard restore and post-reload persistence. **Two implementer decisions were better than the packet specified.** First, Gate 4's "never delete" is implemented with a `restoredAt` marker on the slot rather than deletion, which keeps the recovery copy *and* stops the notice recurring forever — the packet did not say how to reconcile those. Second, the `storedVersion === currentVersion` branch now surfaces the notice for an un-restored copy, so a student who reloads after a displacement still sees the offer; without that the notice would have existed only on the single load that performed the displacement.
- 2026-09-01: **Playwright `addInitScript` re-runs on `page.reload()` (test-technique caution).** `plan-119`'s browser spec must prove that a reload keeps the restored program, which is what demonstrates the version key was re-stamped. But `addInitScript` re-executes on every navigation including reload, so the fixture re-seeded the stale hash and re-displaced the workspace, making the assertion meaningless while still passing for the wrong reason. The implementer guarded seeding behind a `sessionStorage` flag so it runs only on first navigation. Any future spec that seeds storage and then reloads to test persistence needs the same guard — otherwise it proves nothing and looks green.

- 2026-09-01: **Third similarity wording (owner, `plan-121` preflight review).** Option 1 produces three outcomes, not two, and the third was never approved. When names are all blank but file identities differ, the group reads **"identical attempt sequence and identical captured program states in separate submissions."** The preflight plan proposed "across distinct submitters," which was rejected: two differing filenames prove two separate files, not two different people. A teacher may have renamed files, or one student may have submitted twice under different names, and in both cases that sentence is false while reading as authoritative. Overclaiming in an academic-integrity signal is the exact defect class `plan-121` exists to remove, so the fix must not reintroduce it. The other two wordings are unchanged: distinct typed names keeps the existing "under different names" text, and indistinguishable file identities keeps the approved "submitters not distinguishable from these files."
- 2026-09-01: **Byte-identity claims need a captured fixture, not an assertion (orchestrator addition, `plan-121`).** "Direct-mode payload is byte-identical before and after" cannot be shown by a post-packet test comparing post-packet code to itself. The packet now requires capturing the payload from pre-packet code — fixed session id, fixed `exportedAt`, fixed name — committing it as a fixture, and asserting deep equality against it, with the capture commit recorded in the progress report. Generalizes to any future refactor whose contract is "output does not change."
- 2026-09-01: **Do not duplicate the integrity hash helper (`plan-121`).** `computeBrowserSha256Hex` already exists privately at `src/usage/usageTracker.js:198`. The preflight plan proposed implementing and exporting a second copy in `usageFormat.js`, which would give the integrity contract two definitions of its own hash. Resolution: `buildExportPayloadWithIntegrity` takes `computeSha256` as an injected parameter and the tracker passes its existing function; if a shared definition is wanted, move it and leave no copy behind.

- 2026-09-01: **`plan-121` complete.** Verified independently: the pre-packet fixture was captured and committed at `315a776` *before* the implementation commit `34cbd14`, so direct-mode byte-identity is falsifiably pinned rather than asserted; `computeBrowserSha256Hex` was moved to `usageFormat.js` with no copy left in `usageTracker.js`, so the integrity contract has one definition of its own hash; the three owner-approved similarity wordings are byte-exact and identical in both analyzer copies; `submission-N` is proven not to satisfy distinctness (`distinctSubmitters: false`), and the adversarial fixture uses real filenames that *look* positional (`submission-alice.json`); the entry API carries one `wording` field and no `description`; the sentinel search has its mirrored positive assertion on direct mode. 595/595 unit tests, clean build, 21/21 browser tooling. Scratch directories the implementer created inside the repo tree were removed and the tree is clean.
- 2026-09-01: **`plan-116` gate cleared (owner) — four counters, not two.** Resolved against the real taxonomy in `src/core/events.js`. **Collisions split on the existing `reason` field** of `runner.blockedOrBounced`: runner-collision bounces (`turnEngine.js:546`) and map-blockage bounces (`:530`) are counted separately. A single combined counter was rejected because "no collisions" would then mean "never bumped anything," which on a living board with a Charger may be near-impossible — non-discriminating, and contrary to the plan-113 standard. **Waste is also two counters:** resource-unavailable attempts from `resource.unavailable` (already emitted at three sites; maps exactly to the existing `resource_no_readiness_guard` learning moment), and ineffective-freeze uses where the affected set is empty — the engine already computes `affectedRunners` (`turnEngine.js:156`) but does not emit its size, which is the one new piece of instrumentation this packet needs. **Unused-barrier waste is excluded**: a barrier nothing passed did nothing measurable yet may have deterred an enemy that never came, and a contested definition must not be baked into a counter a later criterion will treat as fact.
- 2026-09-01: **Tracking packets record distinguishable things distinguishably (principle).** `plan-116`'s four-counter shape follows from its own scope: it is tracking-only, and the criteria decision belongs to a later authoring packet. Collapsing a dimension now would foreclose that choice and cost another tracking packet to recover. Apply this to any future instrumentation packet whose consumer has not been written yet — do not pre-aggregate on behalf of a decision you are not making.

- 2026-09-01: **RETRACTED by `plan-122` Amendment 01 — see the entry below. Kept for the record because the reasoning error is the useful part.** ~~Smoke tier drops to `workers: 1`.~~ A push failed CI on the `key-capture-passthrough` D-key test with a 30s `waitForFunction` timeout, blocking the Pages deploy and therefore Gate 1 (`deploy-pages.yml`'s deploy job has `needs: build`, and build runs smoke). Diagnosed at orchestration by reproduction: the spec passes alone (10/10, 17.2s), the test passes alone three times (~5.7s), the **full smoke suite fails at the default `workers: 2`** (60 passed, 1 failed — the same test), and the **full suite passes at `--workers=1`** (61/61). The trigger is CPU contention, not CI, not a timeout, and not a keybinding. The failure snapshot also shows neither Stage 0 notice visible and focus correctly on `Reset Level`, clearing the `plan-118`/`plan-119` UI additions. Measurement decides the fix: the suite takes 1.0m at two workers and 1.1m at one, so roughly six seconds of parallelism was buying a deploy-blocking flake. **Stage 0 did not break the test** — it added a startup storage probe, a guided-load slot read, and a hidden element, which together pushed an already-marginal `workers: 2` race over the edge.
- 2026-09-01: **Rejected: moving the spec out of smoke.** The `blockly-trace-playback.spec.js` precedent (excluded from smoke for "CPU-contention flake at workers: 2") looked applicable but is not: `ci.yml` runs only `npm test`, `build`, `test:browser:smoke`, and `test:browser:focus`. **The release tier is not run in CI.** Moving a spec to extended/release silently stops it running in CI, trading a visible failure for invisible loss of coverage — here, on the real-browser key pipeline. Any future "move it to extended" remedy must check which tiers CI actually runs first.
- 2026-09-01: **Rejected: raising the timeout, as the GitHub explainer recommended.** The test passes alone in 5.7s; it is not slow, it is losing a race. A larger timeout makes a flake rarer and less legible without removing it, on the tier that gates deployment. Also rejected: `console.log` inside a `waitForFunction`, which executes in page context and never reaches the runner output. Standing caution — an automated CI explainer that cannot reproduce a failure tends to recommend widening tolerances; treat that as a prompt to reproduce, not as a diagnosis.
- 2026-09-01: **A test tier's stated justification is a maintenance obligation.** `playwright.smoke.config.js` justified `workers: 2` with "this file set has no timing-sensitive animation tests." That premise silently became false as the file set grew, and the comment would have led the next author to restore the setting. A test that dispatches real browser input and then waits on resulting app state is timing-sensitive under contention even with no animation in it. When a tier's stated premise is falsified, correct the comment in the same change as the setting.

- 2026-09-01: **`plan-122` rediagnosed — an animation-frame race, not concurrency. The orchestrator's original diagnosis is retracted.** The implementer's stop condition fired and refuted it: the full smoke suite failed **twice** at `--workers=1`, and the single test alone at `--repeat-each 10 --workers=1` gave 9 passed / 1 failed — a **~10% intrinsic flake with no concurrency at all**. Confirmed mechanism, verified independently by the orchestrator: `p5.noLoop()` does not cancel an already-queued `requestAnimationFrame`; the key press synchronously sets `queuedActionForCurrentRunner` and moves to `PROCESSING_ACTION`; if the pending frame fires before Playwright's `waitForFunction` begins polling, `processTurnActions` (`src/core/turnEngine.js:771-773`) calls `executeQueuedAction` (`:468`) which clears the field at `:651`; the predicate can then never become true. The fix is to drain the pending frame after `noLoop()` and before the key press. **The worker-count change is deferred, not adopted** — its justification was the retracted diagnosis; the stale config comment is still corrected because the test is timing-sensitive either way.
- 2026-09-01: **How the orchestrator got it wrong (falsification-check failure, named with its instance).** The retracted diagnosis ran **one** suite execution per worker count, observed `workers: 2` fail and `workers: 1` pass, and treated the pair as a contrast. Against a ~10% per-run flake, a single passing run is precisely what noise looks like. Both rival hypotheses — contention versus an intrinsic race — predicted the `workers: 2` failure, and the experiment gave the intrinsic-race hypothesis no chance to be falsified because each arm had a sample size of one. **Standing rule: before attributing a test failure to a condition, run each arm enough times to distinguish the condition from the flake rate; a single pass is not a control.** The implementer's `--repeat-each 10` was better methodology than the packet asked for, and its stop condition is what caught this.
- 2026-09-01: **Stability evidence must be proportionate to the measured flake rate.** `plan-122` originally required three passing suite runs. Against a ~10% per-run flake that leaves roughly a 25% chance of a false all-clear. Amendment 01 requires 20 repeats of the single test at each worker count plus two full-suite runs. Generalize: when a fix targets a flake, size the confirmation against the observed rate rather than picking a round number, and never re-run until it passes.
- 2026-09-01: **A test that waits on a transient engine field races the render loop by construction.** `queuedActionForCurrentRunner` exists only between the key press and the next processed frame. This is worth remembering beyond `plan-122`: any future browser test that dispatches input and then waits on intermediate turn-engine state needs the same animation-frame drain, or should assert the applied outcome instead.

- 2026-09-01: **`plan-122` complete; the deferred worker-count question (N2) is resolved as keep `workers: 2`.** The frame-drain fix is stable at both worker counts — implementer 20/20 at each plus 61/61 twice, orchestrator independently 20/20 at default workers and 61/61 full smoke, 595/595 unit, clean build. With the race gone there is no remaining reason to serialize the tier, so the parallelism is kept and the stale justification comment is corrected regardless. The fix is deterministic rather than probabilistic: the drain's own `requestAnimationFrame` callback queues behind p5's pending `_draw`, so that last frame runs before anything is queued and, with `_loop` false, none is scheduled after. **The Pages deploy and Gate 1 are unblocked.**
- 2026-09-01: **The retracted diagnosis had a second bad measurement, worth recording with the first.** Its "six seconds of parallelism" figure came from single suite runs timed at 1.0m and 1.1m. Post-fix runs of the same suite take ~37s on the same machine, so those numbers were noise-dominated by machine load and should never have justified a config change either. Same error class as the sample-size-of-one attribution: a single measurement treated as signal. Deferring N2 rather than bundling it with the fix is what kept a badly-founded config change out of the repository.

- 2026-09-01: **First live Gate 1 reading — the handshake works; the parent banner lies.** `plan-120` reopened for `repair-03`. The implementer's diagnosis is correct and was verified independently: the child's `parentOriginMessage` row is assigned only inside the `BBA_PROBE_CONTEXT` handler and only reaches `pass` when `event.origin` matches the parent's self-reported origin, so a `pass` there proves the full round trip; meanwhile the shell's `load` handler unconditionally overwrites the success banner with "waiting…". The detail that explains why it never recovers: the handshake handler calls `clearTimeout(handshakeTimer)` *before* setting success, so once `load` reverts the banner nothing will ever correct it. Fix is a `handshakeReceived` guard. **Readings taken under the stale banner are valid** — the child's rows are the data.
- 2026-09-01: **Version-as-provenance has a stated display-only exemption (`repair-03`).** `repair-02`'s rule is that a behavior change requires a version bump. It is deliberately not applied to the banner guard, because the change alters no measurement, no classification, and no field of either result block, and bumping would invalidate readings the owner is collecting right now. `plan-120-v2` is kept, with the exemption stated in the README and progress report. The boundary is explicit: if the repair touches the protocol — including the optional child→parent acknowledgement — the version bumps to v3 and the exemption lapses.
- 2026-09-01: **First observed HtmlService parent origin, and why one reading decides nothing.** The child reported the parent as `https://n-<opaque-token>-1lu-script.googleusercontent.com`, corroborated independently by `document.referrer` and by `event.origin`. This is the datum the whole parent-authentication dispute turns on, and **one reading cannot answer it** — the four-condition protocol (reload, second signed-in user, new version of the same deployment, new deployment) exists precisely because the token's stability, not its value, is the finding. Consequences, so the next thread does not have to re-derive them: stable across all four means exact origin pinning works and the first Stage 1 protocol packet stays modest; varying by user kills pinning and forces the server-issued deployment/account-bound bootstrap proof, which is a substantially larger packet; varying by version or deployment makes pinning an operational trap requiring a re-pin on every deploy.
- 2026-09-01: **Do not convert a plausible sandbox story into a recorded finding.** The child honestly reports the inherited GAS sandbox as `unknown`, which the packet anticipated. A suggested explanation — that GAS supplies `allow-scripts` plus `allow-same-origin` and Chrome merely warns — is plausible and unmeasured. The substitute for introspection is behavioral: blob download from a click handler and from an async callback, `confirm()`, `prompt()`, `speechSynthesis`, and the storage round trips. Those answer the question the token list was only a proxy for. Record their outcomes and leave the token set `unknown`.
- 2026-09-01: **Usable viewport may be the binding constraint, not any API question.** The first run reported an inner viewport of 1066x620 on the owner's own display. The target is a 1366x768 managed Chromebook, where it will be smaller still, and 620px of height already has to hold the Blockly panel and the p5 canvas, which compete for space today in direct mode. Review finding F13 named usable viewport as a real risk alongside the API questions; this reading is the first evidence it may outrank them. Measure it on the actual device class before drawing conclusions.

- 2026-09-01: **`plan-120` complete after `repair-03`.** Verified: the diff is exactly three lines of guard logic — `handshakeReceived` declared, reset on each new child load, and checked in the `load` handler — with nothing else touched, so the display-only exemption to version-as-provenance holds and `plan-120-v2` correctly stays. **One detail the repair note did not specify and the implementer got right:** resetting the flag on each new load, without which loading a second child URL would suppress the legitimate waiting message. The protocol is unchanged (`BBA_PROBE_READY` in, `BBA_PROBE_CONTEXT` out; no acknowledgement added), so no v3 bump. The child's message-origin row is now documented as the authoritative handshake indicator in both the probe README and the operator directions, and the hygiene test pins all three guard lines against regression. Sandbox introspection correctly left `unknown`. 595/595 unit, 6/6 hygiene, clean build. Owner confirmed the deployed shell was re-pasted from the repository with no hand edits, so the provenance stop condition did not apply.

- 2026-09-10: **`plan-116` complete — the last packet from the pre-GAS board.** Verified independently: `applyAreaFreeze` returns `{ effect, affectedCount: affectedRunners.length }` and the emit site takes the count directly, so C1's decoupling from the transient `state.areaFreezeEffect` is real; the counters live inside `emit()`, so bounded-log eviction cannot undercount them; `runnerRole` is resolved by `runnerId` lookup against `state.allRunners` rather than by widening event payloads, which is why the narration contract survived. 608/608 unit tests, clean build, `lint:levels` clean except the two known optional-lab untiered warnings.
- 2026-09-10: **The `plan-116` test file is the standard to point future instrumentation packets at.** Thirteen tests, and crucially **four drive the real engine** through `runGuidedLevelWithSolution` rather than feeding synthetic payloads to the counter helpers: a clean attempt reading zero on all four counters as a negative control, a deliberate map blockage, a deliberate resource exhaustion, and a deliberately engineered ineffective freeze on `freeze-support` at Manhattan distance 5 against `AREA_FREEZE_RADIUS` 2. The freeze case is the one that mattered — `ineffectiveFreezeUses` depends on a brand-new emit-site field, and a synthetic-payload-only test would have passed identically whether or not the engine actually wired it. Instrumentation packets should be asked for this shape by default: the counter that depends on new wiring must be proven through the wiring.
- 2026-09-10: **One event-contract change, recorded because a progress report understated it.** `runner.actionResolved` now carries a conditional `affectedCount`, spread from `details` in `emitActionResolved` and present only when an Area Freeze resolves. The progress report's validation checklist claimed all five canonical payloads "retain exact shape contracts"; that is true of four of them. `docs/subsystems/turn-engine.md` — the authoritative contract — documents the field correctly, so the durable authority was right and only the report was imprecise; corrected inline at review. Worth noting the near-miss shape: `runnerRole` was initially added to *all* payloads, `narration-event-log.test.js` caught it, and the implementer switched to the `runnerId` lookup. The narration test is doing real contract-guard duty and should not be weakened.

- 2026-09-10: **`plan-123` gate cleared (owner) — all five visual-customization items.** (1) Mix-and-match at **slot level** across curated sets. (2) The V2 silhouette lint **suffices**; no separately pinned guided subset — which puts real weight on the lint. (3) "People running" **stays the default**, alternates opt-in; no change for existing students and no screenshot churn in the guides. (4) **Three sets plus the default**, sized so every slot can be checked by hand. (5) Skin choice **rides GAS Stage 2 portable state**. Note the interaction with V5, recorded so it is not misread later: riding portable state does **not** make skin choice evidence. It is carried as resume convenience alongside workspaces and progress, and must never enter the usage export, the fingerprint, or any analyzer path. V1–V12 are settled positions from this date; the charter moves to `ready` and P1–P4 are authorized to be written.
- 2026-09-10: **V13 added — contact-sheet legibility audit (owner proposal).** Render every slot of every curated set into one icon sheet and use image recognition over it to check silhouette distinctness and size consistency. Accepted, because it matches an established repository pattern rather than introducing a new kind of thing: plans 73, 74, 86, and 95 all generate a durable artifact, commit it, and review it. Even with no model involved, a contact sheet lets a human see a silhouette collision instantly, which reading a table of code points cannot. Four constraints decide whether it works: **render at target size** (`CELL_SIZE` is 50, and distinctness at 128px proves nothing about 50px); **render on the target platform**, which converts the owner's own cross-OS caveat into a method — the classroom target is managed Chromebooks with one known renderer, and this is the same discipline the GAS probe established by measuring on a student-OU device; **authoring-time, not runtime**, with the runtime lint keeping only the cheap structural rules; and **the model flags, a human decides**, so the audit produces a reviewable list of suspect pairs rather than an unfalsifiable gate. V13 is a P2 deliverable, not a prerequisite; the fallback is the same sheet reviewed by eye.
- 2026-09-10: **`plan-124` written — star-3 criteria expansion authoring.** Consumes `plan-116`'s counters to close the plan-113 deferral that left star 3 existing only on Phase 6 `both-allies-active` levels. The candidate corpus is the 26 guided levels carrying `turnPar` with no `masteryCriterionId`. The governing constraint is the 2026-08-05 discriminating-power standard applied in the opposite direction from `concept-used`: **if a level has no live enemies, `no-collision` is free**, every passing run earns it, and awarding star 3 for it would be a lie told to a student. Each candidate therefore needs a falsifiable pair — reference solution earns the criterion, deliberately degraded variant still passes but fails it — deliberately parallel to the S8 degenerate-solution standard, where S8 proves a mechanic is *required* and this proves a criterion is *avoidable but achievable*. The packet states outright that **most candidates are expected to be rejected**, and that authoring criteria on a large majority would signal the test was applied too loosely rather than a good result. `mapBlockageBounces` stays deliberately unused: spending it would be a genuine closed-vocabulary expansion needing a charter conversation, whereas `no-collision` and `no-wasted-resource` were pre-authorized by plan-113 and charter S6.

- 2026-09-10: **`plan-123` complete.** Docs-only charter, thirteen settled positions, no code touched. Two of the findings do the real work downstream: V7, that **no canvas palette exists at all** — roughly 29 inline `p.fill`/`p.stroke` sites across four render files — which makes palette extraction a decision-independent prerequisite rather than a nicety; and V8, that light/dark is three separate systems, with Blockly having no theme configured at all, which is why the difficulty ordering is counterintuitive (glyph sets easy, light/dark hardest, sprites deferred). P1-P4 are authorized to be written; P5 stays gated.

- 2026-09-10: **`plan-124` gate cleared (owner) — all three recommendations accepted as written.** `no-collision` reads `runnerCollisionBounces === 0` and only that, which preserves the runner-collision / map-blockage split made on 2026-09-01 rather than undoing it the same month. `no-wasted-resource` reads `resourceUnavailableAttempts === 0 && ineffectiveFreezeUses === 0`, treating the two mistakes students actually make with resources — using what you do not have, and using what you have on nothing — as one discipline rather than two criteria that would mostly restate each other on any given board. The nine `both-allies-active` levels stay out of scope, because `masteryCriterionId` is a single string and giving one of them a new criterion means *removing* a criterion that already works. Dispatched.

- 2026-09-10: **Incoming CourseVGD proposal accepted as `plan-125` — worksheet-checkpoint querystring parameter.** A teacher composes a URL naming the guided levels their paper worksheet asks about; BB prompts the student to look at the paper on completion of those levels. Accepted on the requester's own argument, which is the right one: the worksheet currently asks a student to track which level they are on and remember to stop, while learning a new programming environment, and **the software is in a much better position to know that than the student is.** The zero-coupling property was accepted as a **requirement** rather than an intention and written into the non-goals — if BB ever needs to know what the worksheet says, the design has gone wrong — which is what turns a course-specific favor into a general capability: an educational tool attachable to any piece of paper.

- 2026-09-10: **Three corrections to the incoming proposal, recorded because two of them are the kind a future thread would repeat.** (1) *"The mechanism already exists" is half true.* `devGuidedLevel` is loaded inside `if (import.meta.env.DEV)` at `src/main.js:217-220`, so it does not exist in production builds; a repo-wide search for `URLSearchParams`, `location.search`, and `location.hash` finds that one file. `worksheetAt` is therefore **the first production querystring parameter in the application** — the code shape is reusable, the surface is new, and the new surface is what needs a contract. (2) **Read `location.search` only, never `location.hash`.** `devGuidedLevelLink.js` falls back to the hash, and copying that shape would be a defect: the GAS Stage 1 design has already ratified that transient secrets go in the URL **fragment** so they are not sent in the `Referer` header, so the fragment is spoken for. This is the clearest case on record of "follow the established shape" being the wrong instruction. (3) **The level id namespace is not flat** — nested tutorial-step and challenge ids (`bughunt-22-trace`, `level-21-advanced-layer`, `show-what-you-know-challenge`) will match a naive resolver, and `bughunt-22-trace` is exactly what a teacher would copy out of a level file, so it must land in the unknown-id path. Resolve against `app.state.levels` only, as the dev shortcut already does.

- 2026-09-10: **Unknown worksheet ids fail loudly to the teacher, and must not become a student-facing failure.** The requester asked for loud failure and was right that a teacher with printed sheets needs the mismatch discoverable in the room rather than after the period. But *loud to whom* is a classroom question: a student handed a teacher's authoring error as a mid-lesson obstacle has a problem they cannot act on. So the notice is a load-time, persistent, non-blocking banner naming the bad ids verbatim (the `plan-118` `#storage-status` shape), phrased as a problem with the **link** rather than with the student's work — and, the item most likely to be gotten wrong, **a bad id must not suppress the good ones.** Four valid ids and one typo still marks the four; refusing the whole list over one typo converts a small authoring error into a silently unmarked worksheet, which is worse than the failure being guarded against. A load-time banner is sufficient precisely because the teacher is the first person to open the URL when they test it, which makes the link self-verifying at the moment that matters.

- 2026-09-10: **Worksheet targeting is assignment configuration, not student preference — so it is not persisted.** The parsed checkpoint list lives for the page session and is re-read from the URL on every load. Configuration belongs in the URL the teacher controls, not in student-side storage they cannot see or clear. Both halves of the alternative have been paid for recently: `plan-119` spent a full packet on stale persisted state displacing current intent, and a student opening a bare URL in March and getting February's checkpoints with nothing on screen explaining why is exactly that failure; and `plan-118` established that under an embedded deployment `localStorage` access can *throw outright*, so persistence is not dependable in the environment this feature is most likely to run in. The accepted cost — prompts are lost on a bare bookmark — is documented rather than engineered around. This is the same boundary `plan-123` V5 draws for skin choice, approached from the other side.

- 2026-09-10: **The GAS coupling for `plan-125` is parameter forwarding, and the requirement is generic.** Under the shell the parent Apps Script page composes the child iframe `src`, so a parameter on the parent `/exec` URL does not reach the child unless the shell forwards it: `?worksheetAt=...` will work against a plain Pages URL and **silently do nothing** against a GAS URL. Deferred out of `plan-125` and recorded in `docs/open-questions.md` with the constraint attached — forward an **allowlisted set** of child-facing parameters, not this one by name (which repeats the conversation for every future parameter) and not an unbounded passthrough (which is an injection surface into the child URL). Also corrected: the querystring-activated GAS embedded mode the owner recalled **is specified nowhere in this repository**; plans 118-122 and the subsystem notes define no such parameter, so `plan-125` is genuinely the first. Stage 1 proper is unwritten and blocked on Gates 1 and 2, so there was no packet to amend — recording the requirement was the whole of the action available.

- 2026-09-10: **Correction — the `plan-124` gate's rationale for `no-collision` was wrong, though the ruling stands.** `runner_collision_bounce` is emitted from one site (`src/core/turnEngine.js:592-605`) and only when the target cell holds a runner that is **on the same team or already frozen**; live enemy contact goes to `resolveCollision` and emits no bounce at all. So `runnerCollisionBounces` counts bumping into your own teammate, or into an enemy you already froze — it never counts enemy contact. The orchestrator's accepted recommendation had claimed runner collision "is the enemy-sensing concept the living-board levels teach," which is false. The *ruling* (exclude `mapBlockageBounces`) survives, and is now evidenced rather than merely reasoned: the reference solution for the only authorable level reads `mapBlockageBounces: 1`, so folding map blockage in would have made that solution fail its own criterion — the exact defect that disqualified `bughunt-28`.

- 2026-09-10: **`no-collision` will not be registered — an unused id under a wrong name is worse than no id.** No candidate level discriminates on it, for the structural reason above, and the name describes enemy avoidance while the counter measures ally obstruction. The packet's own standard — do not award a criterion that discriminates nothing — extends to the registry: do not register an evaluator nothing uses, under a name that misdescribes it. `plan-124` therefore adds exactly one criterion, `no-wasted-resource`, on exactly one level, `show-what-you-know`. If a future level ever discriminates on the ally route, the criterion gets named for what it measures, and that naming is a charter conversation — the same posture already taken toward `mapBlockageBounces`.

- 2026-09-10: **Two of `plan-116`'s four counters end unspent, and one of them was never proven through the engine.** `mapBlockageBounces` was deliberately unused by design; `runnerCollisionBounces` turns out to be unauthorable. Recorded alongside it: `runnerCollisionBounces` was only ever exercised with synthetic payloads. The four engine-driven tests in `tests/unit/attempt-counters.test.js` cover a clean run, a map blockage, a resource exhaustion, and an ineffective freeze — never a real runner-collision bounce. Not a defect in `plan-116` and not cause to reopen it, but a future thread should not assume that counter is proven in situ. The general lesson is narrower than "test more": the 2026-09-10 standard says the counter that depends on new wiring must be proven through the wiring, and this is the counter that quietly was not.

- 2026-09-10: **`bughunt-28`'s reference solution wastes a freeze (observation, not a repair).** Replaying the canonical solution through the harness yields `ineffectiveFreezeUses: 1` — the reference answer to a bug-hunt level fires an Area Freeze that affects nobody. It is why `bughunt-28` cannot carry `no-wasted-resource`: the clean run fails the criterion on its own. Whether a reference solution should model that on a level about a boolean trap is a curriculum question for the owner, deliberately out of scope for an authoring packet, and recorded rather than fixed.

- 2026-09-10: **`plan-124` authoring accepted on independent replay; reopened for the ride-along only.** The falsifiable pair is real and was re-run by the orchestrator rather than read from the report: the reference solution to `show-what-you-know` passes 35/41 for **3 stars**, and the degraded fixture passes 37/41 for **2 stars** — both beat par, so the criterion is the only thing separating the tiers. That is the shape the packet demanded. Also recorded, because it closes a gap noted the same day: the `bughunt-37` degraded run produced **`collide: 9`**, the first `runner_collision_bounce` ever driven through the real engine in a test context, and it occurred on a run that *fails* — which is precisely why the criterion is unauthorable rather than merely unused.

- 2026-09-10: **The fixture extraction reproduced the exact hazard it was authorized to remove.** Extracting the 122-line inline `advanced-scrimmage` derivative into `tests/unit/fixtures/guided-naive-solutions/` changed four tags on the `battlegorithms_value_compare` block from `<value name="LEFT">`/`RIGHT` to `<field name=…>`. Same character count, so the file lengths matched exactly. Blockly **silently discards** them (`Ignoring non-existent field LEFT…`), which dropped the runner-index role split: in the original the two support allies do `STAY_STILL` and are idled, while in the fixture all three allies run the same program and jam into each other **86 times**. Both versions fail the level, so the test — which asserts only `FAILED` — stayed green while its stated premise became false. `docs/open-questions.md` had predicted this verbatim: *"a future fixture repair could leave the test passing while proving something weaker than it claims."* **The lesson is not that the extraction was a mistake; the same corruption inside a template literal would have been harder to see.** The lesson is that an extraction must be verified by diff against its source and the test must assert its own premise — failing the level was too weak to protect the fixture, and it passed for the wrong reason with nobody noticing.

- 2026-09-10: **`plan-124` complete after `repair-01`.** Verified independently rather than read from the report: the fixture is now byte-identical to the pre-extraction inline XML (and the old file contained exactly one inline `<xml>` blob, so the implementer's regex recovery could not have grabbed the wrong one), and replaying the strengthened assertions against the corrupted `f79c591` version shows **three of the four catch it** — the zero-warning check and both `STAY_STILL` assertions fail, while `level FAILED` still passes. That last row is the point: the one assertion that survives the corruption is exactly the weak assertion the test used to have. The packet ships one criterion on one level out of twenty-six candidates, which is the honest yield it predicted.

- 2026-09-10: **The degenerate-fixture open question is closed, by the packet that briefly made it worse.** `tests/unit/star-evaluation-campaign.test.js` no longer embeds a 122-line derivative; it reads `tests/unit/fixtures/guided-naive-solutions/advanced-scrimmage.xml` and asserts its own premise — that support allies 2 and 3 acted and did `STAY_STILL` and nothing else — plus a zero-Blockly-warning check. The general rule worth keeping from the episode: **an extraction is verified by diff against its source, and a test that cannot detect its own premise being false is not protecting the fixture it reads.**

- 2026-09-10: **`optional-inversion-lab.xml` is a reference solution nothing has ever run.** A corpus-wide audit during `plan-124` found 68 of 69 XML fixtures load clean; the 69th throws `Invalid block definition for type: battlegorithms_boolean_square_ahead_blocked`, a block registered nowhere in `src/`. Nothing catches it because the reference-solution driver filters `levelKind === "prediction"` and the inversion lab is a prediction level. Recorded as an open question rather than fixed — `repair-01` said explicitly that other dirty fixtures were a separate packet. Noted for accuracy: the implementer's report says "all 68 standard fixtures clean," which is true of what was scanned but omits that the file was excluded and why. An accurate-sounding total with an unstated carve-out is the kind of claim this project has been repeatedly bitten by; the exclusion belonged in the sentence.

- 2026-09-10: **`plan-126` written and dispatched — canvas palette extraction, `plan-123`'s P1.** The only P-slate item that needs no owner ruling, and the prerequisite that makes light/dark tractable at all. The orchestrator survey found **34 colour call sites across seven files**, not the roughly 29 across four the charter estimated: V7 counted `src/render/` and missed `src/entities/Runner.js`, `Barrier.js`, and `Flag.js`. The packet requires the implementer to re-survey and correct V7 rather than inherit either number. The governing constraint is that **"no visual change" is a claim, not evidence**: the baseline must be captured mechanically and committed *before* the refactor, in the commit order `plan-121` used for export byte-identity, because a baseline captured afterward proves only that the code agrees with itself. Two further guards: tokens are named for role rather than appearance, since a hue-named token must be renamed by the very change the extraction exists to enable; and the palette carries no theme conditional, because a half-built `dark` branch invites a later packet to assume it works.

## Proposed but not yet accepted

- None currently recorded.
