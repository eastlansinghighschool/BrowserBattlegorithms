# Open Questions

Track only the short list of unresolved choices that future packets or orchestration reviews need to settle.

- Which packet families should be migrated to Bootstrap frontmatter first in Plan 88?
- Do we want to adopt Bootstrap agent-starting prompts as-is, adapt them, or keep the current repo-specific prompt flow?
- Should any packet docs remain permanently manual instead of moving into generated index management?
